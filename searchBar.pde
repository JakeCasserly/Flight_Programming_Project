// Jake Casserly's code for searchBar 20/03/2024

class searchBar extends Widget {
  
  private String result;
  
  public searchBar(float x, float y, float width, float height, String label, color widgetColor, int screenNumber, String result) 
  {
    super(x, y, width, height, label, widgetColor, screenNumber);
    
    this.result = result;
  }
  
  void adjustText() {
    //if (searchBar.checkBorder(mouseX, mouseY)) {
      
    //}
  }
}
